# Generated by ReXGlue v0.8.0 on 2026-07-11 20:15:08 UTC.

[project]
name = "civrev"
sdk_version = "0.8.0"
game_root = "../../xenon_recomp/work/extracted"

[entrypoint]
file_path = "../../xenon_recomp/work/extracted/default.xex"
out_directory_path = "generated/default"
# functions_xr_gapfill.toml: data-referenced functions (vtables/function-
# pointer tables) invisible to reachability analysis; mined from the
# XenonRecomp corpus by tools/gen_missing_functions.py. First runtime hit
# without it: FATAL unregistered call at 0x82E80DA8 during module launch.
# switch_tables_xr.toml: jump tables inside gap-fill functions, mined from the
# XenonRecomp corpus by tools/gen_switch_tables.py (REGENERATE, do not edit —
# the switch-tables session left it uncommitted, which made codegen
# non-reproducible: without it, 92 'unresolved at bctr' REX_FATAL sites appear
# and boot dies at bctr 0x825033AC).
# switch_tables_manual.toml: live-memory-verified tables for the 12 middleware
# message dispatchers ReXGlue under-populates (1 of 221 cases -> ud2 spin on
# the Loading screen when the game sends any unhandled message id).
# functions_jt_targets.toml: jump-table case targets codegen can't classify
# (neither intra-function labels nor known functions) declared as sizeless
# functions, iterated to fixpoint — the tail-call emission reproduces the
# original jump semantics. Regenerated by the fixpoint loop in
# tools/dump_switch_tables_live.md whenever codegen reports
# 'Jump target ... unresolved at bctr'.
includes = ["functions_xr_gapfill.toml", "functions_branch_targets.toml", "switch_tables_xr.toml", "switch_tables_manual.toml", "functions_jt_targets.toml"]

# Manual function boundaries. ReXGlue's analysis left 3 tail-call targets out of
# any function (each caller is a 4-byte `b <target>` thunk). Boundaries taken
# from the XenonRecomp reference corpus (recomp_output/), which resolved all
# three; sizes = next-function start minus target start, cross-checked against
# the translated bodies' final blr/b.
[entrypoint.functions]
0x827F8840 = { size = 0x14 }   # CRT helper: cmplwi/bne + tail b 0x827F0148; target of b from 0x824683E0
0x82324B48 = { size = 0x23C }  # target of b from 0x82324730
0x82841038 = { size = 0x108 }  # leaf float routine; target of b from 0x8286B1F0
