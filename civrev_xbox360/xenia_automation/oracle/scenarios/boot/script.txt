# boot scenario — take the game from launch to the MAIN MENU.
#
# Verified reliable path (2026-07-11, xenia 7acf88d + virtual pad): do NOT skip
# the intro — pressing a button mid-CG can freeze Xenia's Bink video decode.
# Instead let the logos + intro CG play out and detect the static "Press START"
# title screen (wait_stable), then advance the title (START) and decline the
# sign-in prompt (B) — we have no Xbox profile.
#
# Input goes through the virtual gamepad (virtpad.py / input.sh).

wait  Hardware scaler|Shader .* translated successfully  90
wait_stable 6 150      # logos + intro CG finish; "Press START" title is static
shot 01_title
input START            # -> "Would you like to sign in?"
sleep 3
input B                # A=Yes / B=No -> decline (no profile) -> main menu
sleep 4
shot 02_main_menu

expect_no_crash
